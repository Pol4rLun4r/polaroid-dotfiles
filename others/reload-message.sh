#!/bin/bash

CURRENT_DIR=$(dirname $(realpath "$0"))

source "$CURRENT_DIR/script-message.sh"

centralizar_texto_highlights-2 "Após ter reiniciado a sessão, por favor, inicie o script novamente por meio do comando/atalho: dot-install, no terminal para concluir as instalações."